name: 'banking_dbt'
version: '1.0.0'


profile: 'banking_dbt'


model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"


models:
  banking_dbt:

    staging:
      +schema: STAGING
      +materialized: view

    marts:
      +schema: MARTS
      +materialized: table

    OBT:
    +schema:  OBT
    +materialized: table

snapshots:
  banking_dbt:
    +target_schema: ANALYTICS_SNAPSHOTS